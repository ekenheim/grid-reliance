# Python packages installed into the Spark 4.0.2 executor image.
# Must be available on both driver and executor pods (same image).

# S3 access (Bronze read, Silver write via boto3)
boto3>=1.42.51
botocore>=1.35

# ERA5 NetCDF processing
xarray>=2024.1
netCDF4>=1.7.0
numpy>=1.26
scipy>=1.12          # xarray fallback engine and spatial utilities

# Silver write (pandas + pyarrow; avoids hadoop-aws/s3a dependency)
pandas>=2.2
pyarrow>=17.0

# Bronze fetch scripts (fetch_era5_nordic.py, fetch_entsoe.py)
# Used when the data-engineering image runs as a K8s Job for bootstrap seeding.
cdsapi>=0.6.0         # ECMWF CDS API client for ERA5 downloads
python-dotenv>=1.0    # .env loading in fetch scripts
