# Python deps pre-installed in the Ray worker image.
# Versions pinned to be compatible with:
#   - rayproject/ray:2.54.0 base (Python 3.10, pandas 1.5.3)
#   - zarr>=3.1.0 installed first (nutpie hard requirement)

# Bayesian model stack
pymc>=5.27.1,<6
nutpie>=0.16.0
arviz>=0.23.4,<0.24

# Data handling (pandas is already in the base image; pyarrow and numpy are not)
numpy>=1.26
pyarrow>=14.0.0

# S3 access for model artefact loading on workers
boto3>=1.34.0
