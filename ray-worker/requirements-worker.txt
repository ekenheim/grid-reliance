# Python deps pre-installed in the Ray worker image.
# Base: rayproject/ray:2.54.0 — Python 3.10, pandas 1.5.3.
#
# nutpie is NOT included: nutpie>=0.16 requires zarr>=3.1.0 which requires
# Python >=3.11.  numpyro (JAX-backed NUTS) is used instead — same speed
# class as nutpie, no zarr dependency, works on Python 3.10.

# Bayesian model stack
# PyMC >=5.27.0 requires Python >=3.11 — pin to last Py3.10-compatible release.
# Other components (pipeline, inference) use pymc>=5.27.1,<6 on Python 3.11+.
# This version split is intentional; numpyro NUTS provides equivalent inference.
pymc>=5.20.0,<=5.26.0
numpyro>=0.14.0       # JAX-backed NUTS sampler (replaces nutpie on Py3.10)
jax>=0.4.25
jaxlib>=0.4.25
arviz>=0.19.0,<0.24   # 0.19+ supports PyMC 5.x; <0.24 keeps API stable

# Data handling (pandas is already in the base image; pyarrow and numpy are not)
numpy>=1.26
pyarrow>=14.0.0

# S3 access
boto3>=1.34.0
