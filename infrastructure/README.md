# Infrastructure

Flux Kustomizations for bootstrapping MinIO, Ray Operator, and Spark Operator on the Talos Kubernetes cluster. Declarative GitOps: apply Flux sources and kustomizations to provision the compute and storage layer.

## Run

See "How to Run" in the root [README](../README.md).
